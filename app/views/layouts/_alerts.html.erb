<% flash.each do |name, message| %>
    <%= content_tag :div, :class => "alert alert-dismissable #{alert_contextual_css_class(name.to_sym)}" do %>
        <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
        <%= raw message %>
    <% end %>
<% end %>